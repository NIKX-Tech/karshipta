# builds
build/
gateway/gen/
console/node_modules/
console/.svelte-kit/
console/build/
console/src/lib/gen/

# gateway generated state (operator-facing config in gateway/config/ stays
# tracked; these are runtime output, not committed assets)
gateway/config/fleet_state.yaml
gateway/config/fleet_zones.db
gateway/config/fleet_missions.db

# relay device credentials (secrets, provisioned out of band against a real
# relayly server - never committed; gateway.yaml itself stays safe to commit
# since it only holds the non-secret relay URL/path to this file)
gateway/config/relay_credentials.yaml

# local notes
CLAUDE.local.md

# tooling
.DS_Store
*.log
.env
.env.*
compile_commands.json
.cache/
/.idea
/gateway/.gitignore

/.claude/
